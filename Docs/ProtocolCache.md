# ProtocolCache
(namespace **UnitTestingFramework.Protocol.Data**)

The `ProtocolCache` class contains both the [ParametersCache](./ParametersCache.md) and the [TablesCache](./TablesCache.md) as well
as the ProtocolModel to be used. It keeps the information about the Parameters and Tables existent in the provided protocol.